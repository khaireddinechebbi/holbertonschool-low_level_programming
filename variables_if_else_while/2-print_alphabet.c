#include <stdio.h>
#include <string.h>
/**
 * main - print if number is positive
 * Return: Always 0 (Success)
*/
int main(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
