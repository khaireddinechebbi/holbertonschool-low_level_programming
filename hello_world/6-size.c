#include <stdio.h>
/**
 * main - entry point
 * Description: 'prints the size of various types of char'
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("Size of a char: %lu byte(s)\n", sizeof(char));
return (0);
}
