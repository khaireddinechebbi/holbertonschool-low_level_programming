#include "main.h"
/**
 * _islower - print if number is positive
 * Description: 'alphabet'
 * Return: 1 if c is a lowercase, otherwise 0.
*/
int _islower(int c)
{
int c;
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}

