#include "main.h"
/**
 * main - print if number is positive
 * Description: 'alphabet'
 * Return: 1 if c is a lowercase, otherwise 0.
*/
int main(void)
{
char c;
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
